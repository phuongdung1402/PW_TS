# name: Demo CI
# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v4
#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
#     - name: Install dependencies
#       run: npm ci
#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps
#     - name: Run Playwright tests
#       run: npx playwright test
#     - uses: actions/upload-artifact@v4
#       if: ${{ !cancelled() }}
#       with:
#         name: playwright-report
#         path: playwright-report/
#         retention-days: 30


name: Demo CI

on:
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]

  #cho phép chạy thủ công
  workflow_dispatch:

jobs:
  test:
    name: Job 1 - Chạy test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Step 1 - Check out code
      run: echo "Đang checkout code từ repository...."

    - name: Step 2 - Cài node.js
      run: echo "Cài đặt nodejs"

    - name: Step 3 - Cài dependencies từ package.json
      run: echo "npm install"

    - name: Step 4 - Cài playwrighrt browser
      run: echo "npx playwright install-deps"

    - name: Step 5 - Chạy test
      run: echo "npx playwright test"

  deploy:
    name: Job 2 - Deploy
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Tạo report
        run: echo "tạo report"


  # hello:
  #   name: Chào mừng ngày b
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: In ra thông báo
  #       run:  |
  #         echo " Xin chào đây là dòng đầu tiên"
  #         echo " Xin chào bạn nhé!"
  #         echo "Thời gian : $(date)"
  #         echo "Người push ${{github.actor}}"
  #         echo "Repository ${{github.repository}}"